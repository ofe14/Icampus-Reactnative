import { AppRegistry } from 'react-native';
import App from './App'; // Adjust the path based on your project structure
import { name as todoz } from './app.json';

AppRegistry.registerComponent(todoz, () => App);
